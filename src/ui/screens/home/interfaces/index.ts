import { ScreenProps } from '../../../../navigation/interfaces';

export enum ProductFilter {
  ALL = 'all',
  WON = 'won',
  REDEEMED = 'redeemed',
}

export interface UseHomeProps extends ScreenProps {}
