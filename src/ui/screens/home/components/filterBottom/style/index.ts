import { ScaledSheet } from 'react-native-size-matters';

export function FilterBottomstyles() {
  return ScaledSheet.create({
    containerButtonsStyle: {
      flexDirection: 'row',
      gap: 10,
    },
  });
}
